import configureMockStore from "redux-mock-store"
import thunk from "redux-thunk"
import nock from "nock"

xdescribe("Profile Actions", () => {
  let store
  let expectedActions
  beforeEach(() => {

  })
  afterEach(() => {
    nock.cleanAll()
  })

  describe("addCities", () => {
    beforeEach(() => {

    })
    it("", () => {})
  })

  describe("removeCities", () => {
    beforeEach(() => {

    })
    it("", () => {})
  })
})
